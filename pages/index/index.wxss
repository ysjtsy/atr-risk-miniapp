/* 引入全局基础样式（路径按你的实际来，如果 styles 在项目根目录） */
@import "../../styles/base.wxss";

/* 可以加一点页面级的小调整 */
.container {
  /* 可选：稍微窄一点，看起来更像工具面板 */
  max-width: 680rpx;
  margin: 0 auto;
}

/* 给结果卡片一点额外的间距，方便观察 */
.result-card-wrapper {
  margin-top: 24rpx;
}
