<view class="section">
  <view class="label">{{ label }}</view>

  <!-- 可选的副标题 -->
  <view class="sub-label" wx:if="{{ subLabel }}">
    {{ subLabel }}
  </view>

  <!-- 内容插槽 -->
  <slot></slot>
</view>
